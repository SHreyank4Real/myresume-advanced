apiVersion: apps/v1
kind: Deployment
metadata:
  name: resume-app
  namespace: resume
  labels:
    app: resume
spec:
  replicas: 2
  selector:
    matchLabels:
      app: resume
  template:
    metadata:
      labels:
        app: resume
    spec:
      imagePullSecrets:
        - name: pullfrom-ak
      containers:
        - name: app
          image: artifactkeeper.shreyank.in/readonly/myresume:v5
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
              name: http
          envFrom:
            - configMapRef:
                name: resume-config
            - secretRef:
                name: resume-secrets
                optional: true
          readinessProbe:
            httpGet:
              path: /api/stats
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/stats
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: "1"
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: resume
  namespace: resume
spec:
  type: NodePort
  selector:
    app: resume
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30003